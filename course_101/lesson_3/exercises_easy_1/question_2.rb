# Describe the difference between ! and ? in Ruby. And explain what would happen in the following scenarios:

# ! and ? at the end of a method is actually part of the method name
# and not ruby syntax. other uses are Ruby syntax

what is != and where should you use it? # means not equal used to compare
put ! before something, like !user_name # turn any object into the opposite of their boolean equivalent.
put ! after something, like words.uniq! # bang operator usually means it will mutate the operation
put ? before something 
put ? after something
put !! before something, like !!user_name # turns any object into their boolean equivalent.
